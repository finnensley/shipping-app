{
  "version": 2,
  "buildCommand": "npm run build",
  "routes": [
    { "src": "/api/(.*)", "dest": "server.js" },
    { "src": "/auth/(.*)", "dest": "server.js" },
    { "src": "/(.*)", "dest": "/dist/$1" }
  ]
}
