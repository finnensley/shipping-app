import React from 'react'
import { orders } from '../../data/orders'

const singleOrder = () => {

    //get a specific order from the orders file, map over it for display
    // show items with add to package button, moves item to package (just like logic with shopping cart) 
    // try order 100

    
    return (
        <>
        
        
        
        </>

    )

}